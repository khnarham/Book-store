export enum StatusEnum {
    PENDING = "PENDING",
    APPROVED = "APPROVED",
    REJECTED = "REJECTED",
  }
  
  export enum RoleEnum {
    USER = "USER",
    ADMIN = "ADMIN",
  }
  
  export enum BorrowStatusEnum {
    BORROWED = "BORROWED",
    RETURNED = "RETURNED",
  }
  